{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Festival creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_festival') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
